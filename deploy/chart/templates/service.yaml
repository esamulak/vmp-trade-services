apiVersion: v1
kind: Service
metadata:
  name: vmp-trade-services
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vmp-trade-services
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
